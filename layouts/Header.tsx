import { FC } from 'react';

const Header: FC = () => {
  return (
    <div className="h-14 p-2 bg-green-300 flex justify-center items-center">
      Header
    </div>
  );
};

export default Header;
